interface RowType {
  name: string,
  id: string,
  city: string,
  fast: string,
  slow: string,
  status: number,
  now: string,
  fault: string,
  person: string,
  tel: string
}

export type { RowType }
